\usepackage{booktabs}
\usepackage{graphicx}

\setkeys{Gin}{width=\textwidth}
% \usepackage{kpfonts}

\usepackage{fontspec}
% Main (serif) font:
% \setmainfont{Merriweather}
\setmainfont[
  UprightFont    = Merriweather-Light,
  ItalicFont     = Merriweather-Light Italic,
  BoldFont       = Merriweather-Bold,
  BoldItalicFont = Merriweather-Bold Italic,
  Numbers        = {Lining,Monospaced},
]{Merriweather}
% Sans‑serif:
\setsansfont{Roboto}[
  UprightFont    = *-Regular,
  BoldFont       = *-Bold,
  ItalicFont     = *-Italic,
  BoldItalicFont = *-BoldItalic,
  Numbers        = {Lining,Monospaced}
]
% Monospaced:
% \setmonofont{Source Code Pro}
\setmonofont{Source Code Pro}[Scale=0.9]
\setmonofont[
  Scale = 1.0,
  UprightFont = *-Light,
  BoldFont = *-SemiBold,
  ItalicFont = *-Light Italic
]{IBM Plex Mono}

\usepackage{setspace}
\setstretch{1.3}

\usepackage[english]{babel}
\addto\captionsenglish{%
  \renewcommand{\chaptername}{Lecture}%
}

\graphicspath{{fig/}{images/}}

\usepackage{pdfpages}
% get texcount to ignore long tables
%TC:group longtable 1 0 

% --- Page style: header & footer --------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{} % clear defaults

% Left header: subtitle from YAML
\fancyhead[L]{Benjamin Evans (170047831)}

\makeatletter
\renewcommand{\sectionmark}[1]{\markboth{\thesection\enspace #1}{}}
\renewcommand{\subsectionmark}[1]{\markright{\thesubsection\enspace #1}}

\fancyhead[R]{%
  \nouppercase{%
    \leftmark
    \ifx\rightmark\@empty
    \fi
  }%
}
\makeatother

% Footer: centered page number
\fancyfoot[C]{\thepage}
\setlength{\headheight}{15pt}

% listing & packages for code in appendix
% \usepackage{listings}
% \usepackage{inconsolata} 
% \lstset{basicstyle=\ttfamily\footnotesize,breaklines=true,columns=fullflexible,upquote=true,numbers=left,numberstyle=\tiny,frame=single}

\definecolor{myttgreen}{HTML}{188038}
\definecolor{myttred}{HTML}{C62828}
\newcommand{\greentt}[1]{\texttt{\color{myttgreen}#1}}
\newcommand{\redtt}[1]{\texttt{\color{myttred}#1}}

% \let\oldtexttt\texttt
% \renewcommand{\texttt}[1]{\oldtexttt{\color{myttgreen}#1}}


% --- First page: cover sheet --------------------------------
\AtBeginDocument{%
  \includepdf[pages=1]{smm634-IndividualCourseworkSubmissionFormEvansBenjamin.pdf}%
  \clearpage
}